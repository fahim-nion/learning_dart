//list property

void main(){
  var city = ["dhaka","kurigram","bogra","bandorban"];
  print(city.length);
  print(city.reversed);
  print(city.first);
  print(city.last);
  print(city.hashCode);
  print(city.isEmpty);
  print(city.isNotEmpty);
}